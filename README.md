📧 Email Filter from Large CSV

Этот скрипт предназначен для поиска и извлечения строк с e-mail адресами из больших CSV-файлов.
Он безопасно обрабатывает данные поштучно (по частям), чтобы не перегружать память, и показывает прогресс с помощью tqdm.

🚀 Возможности

Обработка очень больших CSV-файлов по частям (параметр chunksize).

Автоматический подсчёт количества строк для отображения прогресса.

Поиск e-mail адресов в столбцах email и email2.

Корректная работа при ошибках чтения (on_bad_lines='skip').

Сохранение результата в новый CSV-файл.

📦 Требования
pip install pandas tqdm

🧩 Использование

Помести свой CSV-файл (например, xaba.csv) рядом со скриптом.

Укажи имя входного и выходного файла в начале кода:

input_file = "xaba.csv"
output_file = "contacts_with_emails.csv"


Запусти скрипт:

python extract_emails.py


Дождись завершения — в консоли появится отчёт вроде:

✅ Готово! Сохранено 52314 строк с e-mail в contacts_with_emails.csv

⚙️ Параметры
Параметр	Описание
chunksize	Размер порции строк, считываемых за один проход. Оптимально 50 000–200 000 для больших файлов.
input_file	Путь к исходному CSV-файлу.
output_file	Имя (или путь) итогового CSV-файла.
🧠 Примечания

Если файл очень большой (десятки гигабайт), можно пропустить подсчёт строк — скрипт всё равно будет работать.

Если названия столбцов отличаются от email / email2, нужно изменить соответствующие строки в коде.

📝 Лицензия

MIT License — можно использовать, модифицировать и распространять свободно.
